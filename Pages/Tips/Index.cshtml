@page
@model SalonWeb.Pages.Tips.IndexModel

@{
    ViewData["Title"] = "Masaj";
}

<h1>Tipuri Masaje</h1>

<p>
    <a asp-page="Create">Masaj Nou</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tip[0].TipMasaj)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Tip) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipMasaj)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
